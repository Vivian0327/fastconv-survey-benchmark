# Loose bounds for convenience. For the EXACT versions used in the paper,
# use requirements-lock.txt instead.
# GPU experiments need a CUDA build of PyTorch, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.2
numpy>=1.24
matplotlib>=3.7
